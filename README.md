ServiceMedia is a content management and publishing system for immersive media.  

Requires nodejs with express, a mongodb-compatible database, and an AWS S3 compatible object store.  Also an instance of the media utilities server smxr-utils for media encoding. 

To install, run 'npm install' and create a .env file populated with the values shown in dotenv.txt.   